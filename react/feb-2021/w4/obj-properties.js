// object review

let obj = {
  prop1: "value 1",
};

// do you add a property to the object?

console.log(obj);

// you can also add a property using a variable
let str = "prop3";

console.log(obj);
